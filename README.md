# The-Notepad
# 📝 Python Notepad

A fully functional Notepad clone built with **Tkinter** in Python.  
This project replicates the core features of Windows Notepad with additional enhancements like zoom, word wrap, and recent files.

---

## ✨ Features

- **File Operations**
  - New, Open, Save, Save As
  - Open Recent files list
  - Exit with unsaved changes prompt

- **Edit Operations**
  - Undo / Redo
  - Cut, Copy, Paste
  - Find and Replace (with match case option)
  - Go To Line
  - Select All

- **View Options**
  - Toggle Word Wrap
  - Toggle Status Bar
  - Zoom In / Zoom Out / Reset Zoom

- **Format**
  - Choose custom font family and size

- **Status Bar**
  - Displays line, column, text length, zoom level, and word wrap status

- **Keyboard Shortcuts**
  - `Ctrl+N` → New File  
  - `Ctrl+O` → Open File  
  - `Ctrl+S` → Save File  
  - `Ctrl+Shift+S` → Save As  
  - `Ctrl+F` → Find  
  - `Ctrl+H` → Replace  
  - `Ctrl+G` → Go To Line  
  - `Ctrl+Z` → Undo  
  - `Ctrl+Y` → Redo  
  - `Ctrl+A` → Select All  
  - `Ctrl+X / Ctrl+C / Ctrl+V` → Cut / Copy / Paste  
  - `Ctrl++ / Ctrl+- / Ctrl+0` → Zoom In / Zoom Out / Reset Zoom  

---

## 🚀 How to Run

1. **Install Python 3.x**  
   Make sure Python is installed on your system. You can check with:
   ```bash
   python --version
